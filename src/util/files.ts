import { env } from "@/env";

export function getImageUrl(fileKey: string) {
  // return `${env.NEXT_PUBLIC_BUCKET_URL}/${fileKey}`;
  return "https://image.similarpng.com/very-thumbnail/2020/11/White-and-blue-color-sneaker-shoes-on-transparent-background-PNG.png"
}
